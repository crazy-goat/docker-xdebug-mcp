<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1340 790" font-family="-apple-system, 'Helvetica Neue', Helvetica, Arial, sans-serif" font-size="13" fill="#1A1A1A">
  <defs>
    <marker id="arrow-solid" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M0,0 L10,5 L0,10 z" fill="#37474F"/>
    </marker>
    <marker id="arrow-dashed" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M0,0 L10,5 L0,10 z" fill="#3B7DD8"/>
    </marker>
  </defs>

  <!-- Title -->
  <text x="670" y="34" text-anchor="middle" font-weight="700" font-size="18" fill="#1A1A1A">xdbg — overview</text>

  <!-- =========================================================
       HOST cluster (left)
       ========================================================= -->
  <g id="host">
    <rect x="20" y="60" width="440" height="700" rx="12" ry="12"
          fill="#FFF7E0" stroke="#C9A227" stroke-width="1.5"/>
    <text x="240" y="92" text-anchor="middle" font-weight="700" font-size="15" fill="#7C5E0B">Host (developer machine)</text>

    <!-- agent -->
    <g id="agent">
      <rect x="60" y="120" width="360" height="80" rx="8" ry="8"
            fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
      <text x="240" y="158" text-anchor="middle" font-weight="600" font-size="14">AI agent</text>
      <text x="240" y="180" text-anchor="middle" font-size="12" fill="#455A64">Claude Code / opencode / Cursor</text>
    </g>

    <!-- xdbg (binary) -->
    <g id="xdbg">
      <rect x="60" y="240" width="360" height="330" rx="8" ry="8"
            fill="#FFFFFF" stroke="#C9A227" stroke-width="1.5"/>
      <text x="240" y="270" text-anchor="middle" font-weight="600" font-size="13" fill="#7C5E0B">xdbg — MCP stdio server (Go binary)</text>

      <!-- mcp (left) -->
      <g id="mcp">
        <rect x="80" y="295" width="135" height="115" rx="6" ry="6"
              fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
        <text x="147" y="338" text-anchor="middle" font-weight="600" font-size="13">MCP layer</text>
        <text x="147" y="362" text-anchor="middle" font-size="12" fill="#455A64">JSON-RPC</text>
        <text x="147" y="378" text-anchor="middle" font-size="12" fill="#455A64">over</text>
        <text x="147" y="394" text-anchor="middle" font-size="12" fill="#455A64">stdin/stdout</text>
      </g>

      <!-- dbgp (right) -->
      <g id="dbgp">
        <rect x="230" y="295" width="170" height="245" rx="6" ry="6"
              fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
        <text x="315" y="338" text-anchor="middle" font-weight="600" font-size="13">DBGp client</text>
        <text x="315" y="362" text-anchor="middle" font-size="12" fill="#455A64">breakpoints,</text>
        <text x="315" y="378" text-anchor="middle" font-size="12" fill="#455A64">step, eval</text>
        <text x="315" y="394" text-anchor="middle" font-size="12" fill="#455A64">path</text>
        <text x="315" y="410" text-anchor="middle" font-size="12" fill="#455A64">translation</text>
        <text x="315" y="448" text-anchor="middle" font-size="12" fill="#7C5E0B" font-weight="600">
          fires outbound →
        </text>
        <text x="315" y="462" text-anchor="middle" font-size="12" fill="#7C5E0B" font-weight="600">
          HTTP · exec · DBGp
        </text>
        <text x="315" y="476" text-anchor="middle" font-size="12" fill="#7C5E0B" font-weight="600">
          to container
        </text>
      </g>
    </g>

    <!-- listener -->
    <g id="listener">
      <rect x="60" y="610" width="360" height="80" rx="8" ry="8"
            fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
      <text x="240" y="648" text-anchor="middle" font-weight="600" font-size="14">DBGp listener</text>
      <text x="240" y="670" text-anchor="middle" font-size="12" fill="#455A64">0.0.0.0:9003</text>
    </g>
  </g>

  <!-- =========================================================
       DOCKER CONTAINER cluster (right)
       ========================================================= -->
  <g id="container">
    <rect x="900" y="60" width="440" height="700" rx="12" ry="12"
          fill="#E8F3FF" stroke="#3B7DD8" stroke-width="1.5"/>
    <text x="1120" y="92" text-anchor="middle" font-weight="700" font-size="15" fill="#0D47A1">Docker container</text>

  <!-- php-fpm (top-left, slightly lower than app to avoid stacked arrival lanes) -->
  <g id="fpm">
    <rect x="940" y="180" width="180" height="100" rx="8" ry="8"
          fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
    <text x="1030" y="218" text-anchor="middle" font-weight="600" font-size="14">php-fpm</text>
    <text x="1030" y="242" text-anchor="middle" font-size="12" fill="#455A64">web entrypoint</text>
    <text x="1030" y="262" text-anchor="middle" font-size="12" fill="#455A64">(HTTP from xdbg)</text>
  </g>

  <!-- php app (top-right, higher than fpm) -->
  <g id="app">
    <rect x="1140" y="140" width="180" height="100" rx="8" ry="8"
          fill="#FAFAFA" stroke="#607D8B" stroke-width="1"/>
    <text x="1230" y="178" text-anchor="middle" font-weight="600" font-size="14">PHP app</text>
    <text x="1230" y="202" text-anchor="middle" font-size="12" fill="#455A64">controllers, console</text>
    <text x="1230" y="222" text-anchor="middle" font-size="12" fill="#455A64">commands (run_command)</text>
  </g>

  <!-- xdebug engine (bottom-center, aligned horizontally with DBGp listener) -->
  <g id="xdebug">
    <rect x="970" y="595" width="320" height="120" rx="8" ry="8"
          fill="#FFFFFF" stroke="#3B7DD8" stroke-width="1.5"/>
    <text x="1130" y="637" text-anchor="middle" font-weight="700" font-size="14" fill="#0D47A1">Xdebug engine</text>
    <text x="1130" y="661" text-anchor="middle" font-size="12" fill="#455A64">start_with_request=yes</text>
    <text x="1130" y="677" text-anchor="middle" font-size="12" fill="#455A64">client_host=host.docker.internal</text>
    <text x="1130" y="703" text-anchor="middle" font-size="12" fill="#0D47A1" font-style="italic">dials host:9003 on every request</text>
  </g>
  </g>

  <!-- =========================================================
       WITHIN-HOST arrows (solid)
       ========================================================= -->
  <!-- agent → mcp -->
  <line x1="147" y1="200" x2="147" y2="290" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
  <text x="147" y="430" text-anchor="middle" font-size="12" fill="#263238" font-weight="600">MCP tools:</text>
  <text x="147" y="444" text-anchor="middle" font-size="12" fill="#263238">xdbg_request,</text>
  <text x="147" y="458" text-anchor="middle" font-size="12" fill="#263238">xdbg_run_command,</text>
  <text x="147" y="472" text-anchor="middle" font-size="12" fill="#263238">xdbg_set_breakpoint,</text>
  <text x="147" y="486" text-anchor="middle" font-size="12" fill="#263238">xdbg_eval, …</text>

  <!-- mcp → dbgp -->
  <line x1="215" y1="350" x2="225" y2="350" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
  <text x="220" y="338" text-anchor="middle" font-size="12" fill="#263238">dispatch</text>

  <!-- dbgp → listener (open :9003) -->
  <line x1="315" y1="540" x2="240" y2="605" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
  <text x="345" y="555" font-size="12" fill="#263238" font-weight="600">opens :9003</text>

  <!-- =========================================================
       WITHIN-CONTAINER arrows (solid)
       ========================================================= -->
  <!-- fpm → xdebug  -->
  <line x1="1030" y1="280" x2="1030" y2="590" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
  <text x="950" y="440" font-size="12" fill="#263238">runs script</text>
  <text x="950" y="454" font-size="12" fill="#263238">→ hooks fire</text>

  <!-- app → xdebug -->
  <line x1="1230" y1="240" x2="1230" y2="590" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
  <text x="1240" y="420" font-size="12" fill="#263238">executes</text>
  <text x="1240" y="434" font-size="12" fill="#263238">→ hooks fire</text>

  <!-- =========================================================
       CROSS-CLUSTER arrows (dashed blue) — Manhattan / PCB routing
       Each path: M start  H midX1  V y-target  H target
       ========================================================= -->

  <!-- dbgp → php-fpm (fire HTTP) — label aligned left like run_command -->
  <path d="M 400 450 H 820 V 205 H 935" fill="none"
        stroke="#3B7DD8" stroke-width="1.5"
        stroke-dasharray="6,4" marker-end="url(#arrow-dashed)"/>
  <text x="620" y="432" text-anchor="end" font-size="12" fill="#0D47A1" font-weight="600">fire HTTP request</text>
  <text x="620" y="446" text-anchor="end" font-size="12" fill="#0D47A1">(method / headers / body)</text>

  <!-- dbgp → PHP app (run_command) — vertical riser shifted right to clear label -->
  <path d="M 400 400 H 720 V 165 H 1135" fill="none"
        stroke="#3B7DD8" stroke-width="1.5"
        stroke-dasharray="6,4" marker-end="url(#arrow-dashed)"/>
  <text x="620" y="376" text-anchor="end" font-size="12" fill="#0D47A1" font-weight="600">run_command</text>
  <text x="620" y="390" text-anchor="end" font-size="12" fill="#0D47A1">(docker compose exec)</text>

  <!-- listener ↔ xdebug (DBGp :9003) — main bridge (orthogonal, now aligned horizontally) -->
  <!-- listener ↔ xdebug (DBGp :9003) — two horizontal arrows with wide spacing -->
  <!-- ① Xdebug → listener (init) — line at y=655, label ABOVE -->
  <path d="M 970 655 H 425" fill="none"
        stroke="#3B7DD8" stroke-width="1.8"
        stroke-dasharray="6,4" marker-end="url(#arrow-dashed)"/>
  <text x="697" y="593" text-anchor="middle" font-size="13" fill="#0D47A1" font-weight="700">① DBGp over TCP :9003 — init</text>
  <text x="697" y="607" text-anchor="middle" font-size="12" fill="#0D47A1">Xdebug dials host.docker.internal:9003</text>
  <text x="697" y="621" text-anchor="middle" font-size="12" fill="#0D47A1">on every request / console command</text>

  <!-- ② listener → Xdebug (response tunnel) — line 30px below at y=685, label BELOW -->
  <path d="M 425 685 H 970" fill="none"
        stroke="#3B7DD8" stroke-width="1.8"
        stroke-dasharray="6,4" marker-end="url(#arrow-dashed)"/>
  <text x="697" y="722" text-anchor="middle" font-size="13" fill="#0D47A1" font-weight="700">② session commands (response tunnel)</text>
  <text x="697" y="736" text-anchor="middle" font-size="12" fill="#0D47A1">xdbg → Xdebug: breakpoint_set, step,</text>
  <text x="697" y="750" text-anchor="middle" font-size="12" fill="#0D47A1">eval, run, continue (over the same TCP :9003)</text>

  <!-- legend (bottom of host cluster) -->
  <g transform="translate(60, 710)">
    <line x1="0" y1="0" x2="20" y2="0" stroke="#37474F" stroke-width="1.2" marker-end="url(#arrow-solid)"/>
    <text x="28" y="4" font-size="12" fill="#37474F">in-cluster flow</text>
    <line x1="170" y1="0" x2="190" y2="0" stroke="#3B7DD8" stroke-width="1.5" stroke-dasharray="6,4" marker-end="url(#arrow-dashed)"/>
    <text x="198" y="4" font-size="12" fill="#0D47A1">cross-cluster traffic</text>
  </g>
</svg>
